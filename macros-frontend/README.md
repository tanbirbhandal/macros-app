# GYM Frontend

React + Vite frontend for the Get Your Macros (GYM) app.

## Tech Stack
- React 19
- Vite
- Material UI (MUI)

## Setup

Install dependencies:

```bash
npm install
```

Create a `.env.local` file in this directory

Run the dev server:

```bash
npm run dev
```